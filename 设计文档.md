# Video2Local 设计文档

## 目标

Video2Local 是一个 Python 桌面工具，用于把网页视频下载到本地。当前聚焦抖音收藏页与作者作品页，后续可通过平台适配层扩展到哔哩哔哩、快手和 YouTube。

## 当前范围

- 页面批量同步：收藏页、作者作品页，按顺序处理前 N 个或全部候选视频。
- 单链接解析：支持抖音分享文案、短链和标准视频链接。
- 解析来源：可独立启用站内解析与 Kukutool，结果按清晰度合并展示。
- 下载：选择当前最高可用版本，文件保存到用户选择的目录。
- 归档：可按 `平台/作者` 分目录，或直接平铺到输出目录。

## 关键约束

- 程序启动独立 Chrome profile，不接管用户日常 Chrome。
- 登录、广告、验证码等需要用户在网页中手动完成，程序不绕过第三方服务限制。
- Kukutool 使用专用 Chrome 中已打开的官网页面，模拟填入链接、点击解析、复制清晰度链接；不调用其不稳定的页面内部模块。
- 批量任务串行解析与下载，避免触发平台或第三方服务限流。
- 不使用数据库保存下载记录；输出目录和下载范围由用户控制。

## 架构

| 模块 | 职责 |
| --- | --- |
| `adapters/` | 平台页面识别、候选视频收集、抖音元数据与清晰度解析。 |
| `browser.py` | 专用 Chrome 启动、CDP 连接、Kukutool 网页交互。 |
| `resolvers.py` | 站内与第三方解析来源的统一封装及结果合并。 |
| `downloader.py` | `yt-dlp` 调用、下载与元数据探测。 |
| `archive.py` | Windows 安全文件名、目录规则和文件命名。 |
| `sync_engine.py` | 串行下载、停止请求和同步摘要。 |
| `app_runtime.py` | 应用流程编排、预览与下载选择。 |
| `ui/` | PySide6 窗口、异步控制器与公共结果表。 |

## 核心流程

### 批量预览

1. 用户在专用 Chrome 打开抖音收藏页或作者作品页；若启用 Kukutool，同时打开其官网页面。
2. 程序收集页面候选视频链接，并按用户设置限制数量。
3. 站内解析提取标题、作者和原生清晰度。
4. Kukutool 在同一浏览器标签页顺序解析链接；若出现广告或验证窗口，等待用户处理。
5. 两种来源的版本按清晰度合并，公共结果表只展示本次预览。

### 下载

1. 单链接模式由用户在公共结果表勾选版本后下载。
2. 批量同步按候选顺序解析一个、下载一个，使用合并版本中的最高项。
3. 文件名格式为 `标题-视频ID[-清晰度].扩展名`；`#` 替换为中文逗号，非法 Windows 字符会清理。

## 测试策略

- 测试按模块分文件，避免依赖真实平台网络请求。
- 运行时、解析来源、浏览器交互与 UI 切换均使用模拟对象覆盖成功、回退与失败路径。
- 每次功能调整执行完整 `pytest` 回归。
