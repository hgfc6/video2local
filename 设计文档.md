# Video2Local 设计文档

## 目标

将网页视频以可控、可追溯的方式归档到本地。程序不维护下载数据库；用户通过输出目录、平台工作台和“前 N 个”控制本次任务范围。

## 架构

```text
PySide6 工作台
  ├─ 抖音：native / Kukutool
  └─ Bilibili：native + Chrome cookies
          ↓
平台适配器（页面识别、候选链接、元数据/版本映射）
          ↓
运行时编排（预览、格式选择、顺序队列）
          ↓
yt-dlp / ffmpeg（下载、音视频合并）
          ↓
归档命名（平台/作者或平铺目录）
```

| 模块 | 职责 |
| --- | --- |
| `adapters/` | 平台页面识别、候选链接收集、平台数据映射。 |
| `browser.py` | 独立 Chrome、CDP 连接、cookies 导出、Kukutool 网页交互。 |
| `resolvers.py` | 抖音原生和 Kukutool 解析来源。 |
| `downloader.py` | `yt-dlp` 格式探测与下载；发现 ffmpeg 后传入其目录。 |
| `archive.py` | Windows 安全文件名和输出目录规则。 |
| `sync_engine.py` | 串行下载、停止请求和输出目录根部的文本报告。 |
| `app_runtime.py` | 平台分发、预览、解析源聚合和下载编排。 |
| `ui/` | 平台工作台、异步控制器、共享结果表。 |

## 平台与来源隔离

顶部平台工作台是执行边界，而非解析来源开关：

- 抖音工作台接受抖音页面和分享链接，可启用 `native`、`Kukutool` 或两者；每个版本记录实际来源。
- Bilibili 工作台只接受 Bilibili 页面和链接，固定使用原生解析与专用 Chrome cookies，不调用 Kukutool。
- 工作台切换会清空结果表；平台不匹配的页面或链接直接提示切换，避免跨平台数据混用。

## 核心流程

### 单链接

1. 从分享文案提取平台链接。
2. 根据工作台分发到对应解析器。
3. 返回统一 `VideoMetadata` 和 `VideoVariant` 列表。
4. 用户在结果表选择版本，下载层按选中的直链或格式选择器下载。

### 批量

1. 从专用 Chrome 当前页识别平台和来源类型。
2. 适配器从页面快照收集并去重候选链接，再应用前 N 个限制；Bilibili 额外滚动加载并兼容页面 JSON 中的转义链接。
3. 串行处理每条候选：解析元数据和可用版本，选择最高可用项，随即下载。
4. 单条元数据解析失败时标记为跳过，继续后续条目；预览和同步将页面链接、处理阶段、尝试次数和原始错误写入输出目录的 `.txt` 报告。
5. 停止请求在当前条目完成后生效。

## Bilibili 规则

- 支持标准视频页、`b23.tv`、收藏夹和 UP 投稿页。
- 唯一 ID 使用 BV 号；分 P 使用 `BV号-p序号`。
- 使用 `yt-dlp --dump-single-json` 获得格式列表，选择最高视频流和最佳音频流组合；由 ffmpeg 合并为 MP4。
- Bilibili 解析与下载优先传入专用 Chrome 导出的 cookies；Chrome 不可用时单链接回退匿名解析。

## 约束与测试

- 登录、验证码、广告和第三方页面限制由用户在浏览器中处理，程序不绕过。
- Kukutool 仅在抖音工作台串行使用，避免触发限流。
- 不依赖真实平台网络请求的自动化测试；使用模拟 Chrome、Kukutool 和 `yt-dlp` 输出覆盖成功、回退、来源隔离和 UI 切换。
- 每次变更执行完整 `pytest` 回归。
