# Video2Local 首次使用流程

本文档对应当前 GUI 版本，目标是让你第一次使用时能快速验证整条下载链路，并完成一次真实同步。

## 适用范围

- 当前正式支持：抖音
- 当前支持的内容源页面：
  - 收藏页
  - 作者作品页

后续扩展到哔哩哔哩、快手、YouTube 时，界面流程会尽量保持一致。

## 启动方式

在项目根目录运行：

```powershell
$env:PYTHONPATH='src'
.\.venv\Scripts\python.exe -m video2local.main
```

## 分享链接解析模式

这个模式用于单个视频，不依赖抖音登录。

### 支持的输入

- 纯短链接
- 标准抖音视频链接
- 从抖音“分享 -> 复制链接”得到的整段分享文案

例如：

```text
9.76 复制打开抖音，看看【香菜严选的作品】 https://v.douyin.com/5MF6Y_tP8nk/ 08/05 X@Z.Mj :5pm wse:/
```

### 使用步骤

1. 把分享文案粘贴到输入框
2. 点击 `解析分享链接`
3. 等待表格列出多个清晰度版本
4. 在表格中查看：
   - 清晰度
   - 编码
   - 码率
   - 大小
   - 是否推荐
5. 选中一个版本
6. 点击 `下载所选版本`

### 下载规则

- 当前版本只下载一个你手动选中的版本
- 文件会落到：

```text
downloads/douyin/<author>/<title> [<video_id>] [<quality>].mp4
```

如果视频本身没有可用标题，程序会先回退到 `video_id`。

## 首次使用的推荐顺序

### 1. 启动专用 Chrome

点击 GUI 中的 `启动 Chrome`。

这会打开程序专用的 Chrome 配置目录。这个目录会长期保留，所以你通常只需要登录一次，后续可复用。

### 2. 登录并打开内容源页面

在这个专用 Chrome 窗口里：

- 登录你的抖音账号
- 打开一个可同步页面

支持两种页面：

- 收藏页
- 作者作品页

## 3. 检查当前页面

点击 `检查当前页面`。

如果识别成功，界面会显示：

- 当前平台
- 当前来源类型
- 当前页面 URL

如果失败，通常是以下原因之一：

- 打开的不是受支持页面
- 页面还没加载完
- 当前标签页不是抖音页面
- 登录态失效

## 4. 下载首个样本

点击 `下载首个样本`。

这个动作不会直接开始整批同步，而是只下载当前页面首个可见视频，用来验证以下几件事：

- 页面识别是否正常
- 当前登录态是否可用
- 抖音详情抓取是否成功
- 视频是否能以当前可获取的最高清晰度下载到本地

样本文件会保存到：

```text
downloads/_smoke_test/
```

如果这一步成功，基本说明当前环境已经具备正式同步条件。

## 5. 开始正式同步

点击 `开始同步`。

程序会：

1. 读取当前页面中可见和滚动后出现的视频
2. 解析每个视频的稳定 ID
3. 查询 SQLite，跳过已下载的视频
4. 下载未入库的视频
5. 按平台和作者自动归档

默认归档目录结构如下：

```text
downloads/<platform>/<author_name>/<title> [<video_id>].<ext>
```

例如：

```text
downloads/douyin/h6ii/#情绪 #成长 [7062344670323526953].mp4
```

## 6. 查看进度和结果

同步过程中，GUI 会持续显示：

- 当前状态
- 当前处理的视频作者和标题
- 最近一次摘要或样本文件路径

你还可以使用：

- `打开下载目录`：直接打开本地归档目录
- `查看最近摘要`：查看最近一次同步的统计结果

## 7. 停止与恢复

点击 `停止同步` 后，程序会在当前视频处理完成后安全停止，不会强行中断到不可恢复状态。

如果你后续再次点击 `开始同步`，程序会基于数据库去重：

- 已下载的视频会被跳过
- 未下载的视频会继续处理

## 当前目录说明

- 下载目录：`downloads/`
- 样本验证目录：`downloads/_smoke_test/`
- 数据目录：`.video2local/`
- 数据库：`.video2local/video2local.db`
- 专用 Chrome 配置目录：`.video2local/chrome-profile/`

## 当前版本建议使用方式

每次准备导出一个新页面时，推荐顺序固定为：

1. `启动 Chrome`
2. 登录并打开内容源页面
3. `检查当前页面`
4. `下载首个样本`
5. `开始同步`

这套顺序是当前版本最稳妥的使用方式。
